# النظام الذكي الجديد — إصلاح إعادة الإرسال + بيع للصفقات المفتوحة فقط

## المشكلة القديمة
نظام 4 ساعات كان غير ذكي:
- لو صفقة جديدة حقيقية في نفس العملة في أقل من 4 ساعات → يتجاهلها ❌
- يمنع صفقات حقيقية

## الحل الجديد — نظام البصمة الذكية

### 1. بصمة ذكية للإشارات — يقرأ الإشارات كل مرة ويحدد الجديدة فقط

```python
def buy_fingerprint(plan):
    ticker = plan["ticker"]
    price = round(signal_price, 4)
    sl = round(sl, 4)
    tgt1 = round(tgt1, 4)
    tgt2 = round(tgt2, 4)
    frame = frame
    raw = f"{ticker}_{price}_{sl}_{tgt1}_{tgt2}_{frame}"
    fp = sha1(raw)[:16]
    return fp
```

**كيف يعمل:**
- كل إشارة لها بصمة = العملة + السعر + الوقف + الهدف1 + الهدف2 + الفريم
- إذا نفس البصمة ظهرت مرة ثانية → مكررة → تخطي
- إذا سعر مختلف أو مستويات مختلفة → بصمة مختلفة → جديدة حقيقية → إرسال ✅
- **حتى لو في أقل من 4 ساعات، إذا سعر مختلف → يرسل (يحل مشكلة النظام القديم)**

**مثال:**
- Cycle 1: BTCUSDT 50000 SL49000 T1 51400 → بصمة abc123 → جديدة → يرسل
- Cycle 2: BTCUSDT 50000 SL49000 T1 51400 → نفس البصمة abc123 → مكررة → يتخطى ✅ (يمنع إعادة الإرسال كل دقيقة)
- Cycle 3: BTCUSDT 50500 SL49500 T1 51900 → بصمة مختلفة def456 → جديدة حقيقية → يرسل ✅ (حتى لو بعد دقيقة واحدة)

### 2. نظام تتبع الصفقات المفتوحة مع ترقيم

```python
open_positions = [
  {
    "id": "BTCUSDT_20260920_120000_1_abc",
    "ticker": "BTCUSDT",
    "position_number": 1,  # ترقيم للعملة الواحدة
    "buy_price": 50000,
    "sl": 49000,
    "tgt1": 51400,
    "tgt2": 54000,
    "entry_time": "2026-09-20T12:00:00",
    "status": "OPEN",
    "remaining_pct": 100
  },
  {
    "id": "BTCUSDT_20260920_130000_2_def",
    "ticker": "BTCUSDT",
    "position_number": 2,  # نفس العملة صفقة ثانية
    "buy_price": 50500,
    "sl": 49500,
    "tgt1": 51900,
    "tgt2": 54500,
    "status": "OPEN"
  }
]
```

- عند كل إشارة شراء جديدة → ينشئ صفقة مفتوحة مع ترقيم تلقائي
- نفس العملة عدة صفقات → #1, #2, #3 حسب ترتيب الدخول
- مصنفة حسب سعر الشراء والهدف

### 3. نظام إشارات البيع — للصفقات المفتوحة فقط

```python
def evaluate_sell_signals(store, now):
    for pos in open_positions:
        if status != "OPEN": continue
        current_price = store[ticker]  # سعر حالي من السوق
        
        if current_price >= tgt2:
            sell_type = "T2" → بيع 100% المتبقي
        elif current_price >= tgt1 and not t1_sold:
            sell_type = "T1" → بيع 50%
        elif current_price <= sl:
            sell_type = "SL" → بيع 100% وقف خسارة
        elif hold_time > 5h:
            sell_type = "TIME" → بيع وقت
        
        # يرسل بيع فقط إذا صفقة مفتوحة
        # يعرف أي صفقة يبيع عبر position_number + buy_price
```

**مميزات:**
- ✅ يرسل بيع للصفقات المفتوحة فقط — لا يرسل بيع عشوائي
- ✅ إذا عدة صفقات لنفس العملة → يرقمها ويعرف أي واحدة يبيع حسب سعر الشراء والهدف
- مثال: BTCUSDT #1 شراء 50000 و #2 شراء 50500، سعر حالي 51500 → يبيع #1 T1 فقط (وصل هدفها)، #2 لا تزال مفتوحة
- ✅ بصمة بيع لمنع تكرار نفس إشارة البيع

### 4. واجهة عرض جديدة

```
📡 الإشارات الحية — شراء 2 | بيع 1 | مفتوحة 3
━━━━━━━━━━━━━━━━━━━━
🔴 إشارات البيع — 1 — للصفقات المفتوحة فقط
┌ ✅ BTCUSDT #1 — بيع T1 50%
├ شراء: 50000.0000 → حالي: 51500.0000 (+3.00%)
├ هدف أول 51400.0000
└ صفقة #1 | 1m | GS-T1

🟢 إشارات الشراء الجديدة — 2 — بصمة ذكية
┌ BTCUSDT #3 | GS-T1 | 1m
├ دخول: 49900.0000 (إشارة 50000.0000)
├ وقف: 49000.0000 | هدف1: 51400.0000 هدف2: 54000.0000
└ حجم: 1.5% — جديدة ✅

💼 الصفقات المفتوحة — 3
• BTCUSDT — 2 صفقة:
  #1 شراء 50000.0000 → T1 51400.0000 T2 54000.0000 SL 49000.0000 | 100% | 2026-09-20 12:00
  #2 شراء 50500.0000 → T1 51900.0000 T2 54500.0000 SL 49500.0000 | 100% | 2026-09-20 13:00
```

## الملفات المحدثة
- `TITAN_UNIFIED_BOT.py`:
  - إلغاء نظام 4 ساعات القديم
  - إضافة `buy_fingerprint()` + `filter_new_buy_signals()` → يقرأ كل مرة ويحدد الجديدة فقط
  - إضافة `get_open_positions()` + `create_open_position()` → ترقيم حسب سعر الشراء والهدف
  - إضافة `evaluate_sell_signals()` + `sell_fingerprint()` → بيع للمفتوحة فقط
  - إضافة `LATEST_SELL_PLANS` + `LATEST_OPEN_POSITIONS`
  - تحديث `run_cycle()` → شراء جديد فقط + بيع للمفتوحة فقط
  - تحديث `latest_signals_text()` + `portfolio_text()` + `fmt_engine_status()` → عرض مع ترقيم

## اختبار
```
Test1 نفس الإشارة كل دقيقة: Cycle1 يرسل، Cycle2 يتخطى، Cycle3 يتخطى ✅
Test2 نفس العملة سعر مختلف: يرسل حتى لو بعد دقيقة ✅ (يحل مشكلة 4 ساعات)
Test3 بيع: سعر 51500 → يبيع BTCUSDT #1 فقط، #2 لا → يعرف أي صفقة يبيع ✅
Test3 بيع: ETH بدون صفقة مفتوحة → 0 بيع → لا يرسل إلا للمفتوحة ✅
```

## الخلاصة
- ❌ القديم: 4 ساعات يمنع صفقات حقيقية
- ✅ الجديد: بصمة ذكية = سعر+وقف+أهداف+فريم → يميز الجديد فعلاً حتى لو بعد دقيقة
- ✅ بيع للصفقات المفتوحة فقط مع ترقيم حسب سعر الشراء والهدف
